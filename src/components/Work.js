import * as React from 'react';
import { Container, Grid } from '@mui/material';

export default function Work() {
    return (
        <Grid align='center'>
                    <h2>Sign up</h2>
        </Grid>
    );
}